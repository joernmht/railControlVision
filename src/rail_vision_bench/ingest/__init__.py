"""Ingest stage: image files, video/RTSP frames, screen captures and their preprocessing.

Named ``ingest`` rather than ``io`` so the package never shadows the standard library.
"""

from __future__ import annotations
